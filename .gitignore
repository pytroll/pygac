build
doc/build
*.so
*.pyc
*~
dist
pygac.egg-info
etc/*.cfg
gapfilled_tles/*
pygac/version.py

tmp
.vscode
.venv
*.lock
.python-version

# Phase 0 uncertainty regression: full-tier baselines (multi-MB per orbit,
# regenerated from local LHRR inputs; never committed).
.pytest_cache/pygac-uncertainty-baselines/
